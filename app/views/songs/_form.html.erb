<%= form_for @song do |f| %>
  <% if @song.artist.nil? %>
    <%= artist_select(@artist, @song) %>
  <% else %>
    <%= artist_id_field(@song) %>
  <% end %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <br />

  <%= f.submit %>
<% end %>
